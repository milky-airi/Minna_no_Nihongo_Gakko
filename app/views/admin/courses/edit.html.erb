<div class="container">

  <div class="row">
    <h3>コース編集</h3>
  </div>

  <div class="row">
    <%= form_with model: @course, url: admin_course_path(@course.id), method: :patch do |f| %>

      <div class="field">
        <%= f.label :コース名 %><br />
        <%= f.text_field :name %>
      </div>

      <div class="field">
        <%= f.label :日本語学校 %><br />
        <%= f.collection_select :school_id, School.all, :id, :name %>
      </div>

      <div class="field">
        <%= f.label :入学時期 %>(月)<br />
        <%=  f.select :admission_month, 1..12%>
      </div>

      <div class="field">
        <%= f.label :在学期間 %>（ヶ月）<br />
        <%= f.text_field :duration %>
      </div>

      <div class="field">
        <%= f.label :定員 %><br />
        <%= f.text_field :capacity %>
      </div>

      <div class="field">
        <%= f.label :コースの入学要件 %><br />
        <%= f.text_area :condition %>
      </div>

      <div class="actions">
        <%= f.submit "更新", class: "btn btn-success" %>
      </div>
    <% end %>
  </div>

</div>