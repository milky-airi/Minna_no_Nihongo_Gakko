<div class="container">
  <div class="row">


    <% if @schools.present? %>
      <h3><%= @prefecture %>の日本語学校(<%= @schools.count %>件)</h3>

    <table class="table">
      <tr>
        <td>日本語学校名</td>
        <td>日本語学校名（かな）</td>
        <td>日本語学校名（英語）</td>
        <td>日本語学校情報</td>
        <td>住所</td>
      </tr>
      <% @schools.each do |school| %>
      <tr>
        <td><%= link_to school.name, school_path(school.id) %></td>
        <td><%= school.name_kana %></td>
        <td><%= school.name_en %></td>
        <td><%= school.summary %></td>
        <td><%= school.address %></td>
      </tr>
      <% end %>
    </table>

    <% else %>
      <h3><%= @prefecture %>の日本語学校はありませんでした。他の条件で検索してください。</h3>
    <% end %>

  </div>
</div>